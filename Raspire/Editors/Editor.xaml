<Page
    x:Class="Raspire.Editor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Raspire"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:my="using:Microsoft.UI.Xaml.Controls">
    <Page.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="ListItemV2" x:DataType="local:ClassesLessonUnit">
                <StackPanel Width="135" Height="200" Margin="0,6" Background="{ThemeResource AppBarBackgroundThemeBrush}" CornerRadius="4" PointerEntered="OpenCommander" RightTapped="OpenCommander" BorderBrush="{ThemeResource ButtonDisabledBorderThemeBrush}" BorderThickness="1,0">
                    <TextBlock Text="{x:Bind Form}" Foreground="Gray" HorizontalAlignment="Center" FontSize="10" Margin="4"/>
                    <my:ItemsRepeater ItemsSource="{x:Bind Units}"  >
                        <my:ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="local:LessonUnit">
                                <CheckBox>
                                    <TextBlock Text="{x:Bind ToString()}" Margin="8,1" FontFamily="/Assets/Jost.ttf#Jost" Foreground="DarkGray" PointerEntered="LessonUnitFocused" PointerExited="LessonUnitUnhovered" DoubleTapped="OpenLessonEditor"/>
                                </CheckBox>
                            </DataTemplate>
                        </my:ItemsRepeater.ItemTemplate>
                        <my:ItemsRepeater.Layout>
                            <my:StackLayout Orientation="Vertical" Spacing="2"/>
                        </my:ItemsRepeater.Layout>
                    </my:ItemsRepeater>
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="ItemV2" x:DataType="local:WorkdayClassesUnit">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" CornerRadius="8" BorderBrush="Gray" BorderThickness="1" >
                    <TextBlock Text="{x:Bind Workday}" Width="30" Margin="20" FontSize="20" FontFamily="/Assets/Jost.ttf#Jost" VerticalAlignment="Center"/>
                    <my:ItemsRepeater ItemsSource="{x:Bind ClassesUnits}" ItemTemplate="{StaticResource ListItemV2}">
                        <my:ItemsRepeater.Layout>
                            <my:StackLayout Orientation="Horizontal" Spacing="4"/>
                        </my:ItemsRepeater.Layout>
                    </my:ItemsRepeater>
                </StackPanel>
            </DataTemplate>
            <my:CommandBarFlyout x:Name="Commander" x:Key="Commander">
                <AppBarElementContainer>
                    <Grid>
                        <AutoSuggestBox VerticalAlignment="Center" Margin="8,4"/>
                    </Grid>
                </AppBarElementContainer>
                <AppBarButton Icon="Add" Click="AddLesson" />
            </my:CommandBarFlyout>
            <my:CommandBarFlyout x:Name="LessonCommander" x:Key="LessonCommander">
                <AppBarButton Icon="Edit"/>
                <AppBarButton Icon="Delete"/>
            </my:CommandBarFlyout>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right" Margin="4" HorizontalAlignment="Left">
            <AppBarButton Icon="Document" Label="Файл" />
            <AppBarButton Icon="Print" Label="Печать" />
        </CommandBar>
        <ScrollViewer Grid.Row="1" Margin="4" HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto">
            <my:ItemsRepeater ItemTemplate="{StaticResource ItemV2}" ItemsSource="{x:Bind UnitsWorkdays}">
                <my:ItemsRepeater.Layout>
                    <my:StackLayout Orientation="Vertical" Spacing="8"/>
                </my:ItemsRepeater.Layout>
            </my:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
